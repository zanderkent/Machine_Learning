import pandas as pd
import numpy as np

class custom_accuracy():
    """This is a custom accuracy function that was written for the purpose of this project, 
       I have removed all information here, but this class is still useful, since you can do the same thing here
       and write your own custom function if you want.
    """
    def GetPrediction(model,X,full_data,labels):
        return df
    def suppresed_top_accuracy(preds):
        return(np.array(counts_3), np.array(counts_1))
